annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.15.1
av==18.1.0
click==8.5.0
fastapi==0.141.1
h11==0.16.0
httptools==0.8.0
idna==3.20
numpy==2.5.3
pycryptodome==3.23.0
pydantic_core==2.46.5
pydantic==2.13.5
python-dotenv==1.2.3
PyYAML==6.0.3
starlette==1.6.0
typing_extensions==4.16.0
typing-inspection==0.4.4
uvicorn==0.53.0
uvloop==0.22.1
watchfiles==1.3.0
websockets==17.1

# ---- AI frame enhancer (docs/enhance-ai-spec.md), optional — run tools/install_enhance_deps.sh instead of
# ---- `pip install -r` for this section: basicsr needs a local patch to build on this Python version (see
# ---- that script's comment), so it isn't a plain pinned line below like everything above it.
# torch==2.14.0
# torchvision==0.29.0
# opencv-python-headless==5.0.0.93
# pillow==12.3.0
# basicsr==1.4.2  (patched — see tools/install_enhance_deps.sh)
# facexlib==0.3.0
# gfpgan==1.3.8
# realesrgan==0.3.0
# scipy==1.18.1
# scikit-image==0.26.0
# numba==0.67.0
# filterpy==1.4.5
# addict==2.4.0
# future==1.0.0
# lmdb==2.3.0
# tqdm==4.70.1
# yapf==0.43.0
# tb-nightly==2.21.0a20251023
# requests==2.34.2
# pytesseract==0.3.13  (needs the `tesseract` binary too — tools/install_enhance_deps.sh installs it via Homebrew)
