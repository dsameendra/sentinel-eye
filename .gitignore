.env
bin/go2rtc
*.log
.venv/
data/
__pycache__/
*.pyc
.DS_Store
# facexlib (a GFPGAN dependency — docs/enhance-ai-spec.md) downloads its face-detection/parsing weights
# to ./gfpgan/weights relative to the server's cwd, not somewhere configurable — a few hundred MB that
# should never land in the repo.
/gfpgan/
